#:schema node_modules/wrangler/config-schema.json
name = "wiki"
compatibility_date = "2024-11-18"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".open-next"

# 这一行确保 OpenNext 的资源能被正确找到
[assets]
directory = ".open-next"
binding = "ASSETS"
